{
  "buildCommand": "pnpm --filter keyspot-web build",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "outputDirectory": "keyspot-sdk/apps/web/.next",
  "framework": "nextjs"
}
